import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Superadmin Dashboard">
      <div className="rounded-xl bg-white border shadow-sm p-6">Platform-level controls for users, landlords, and settings.</div>
    </Shell>
  );
}
