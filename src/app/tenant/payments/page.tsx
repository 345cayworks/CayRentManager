import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Tenant Payments">
      <div className="rounded-xl bg-white border shadow-sm p-6">Tenant payment history and outstanding balances.</div>
    </Shell>
  );
}
