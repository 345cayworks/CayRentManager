import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Settings">
      <div className="rounded-xl bg-white border shadow-sm p-6">Workspace and security settings.</div>
    </Shell>
  );
}
