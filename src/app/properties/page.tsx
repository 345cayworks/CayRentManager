import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Properties">
      <div className="rounded-xl bg-white border shadow-sm p-6">Property CRUD with archive lifecycle (no hard delete).</div>
    </Shell>
  );
}
