import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Units">
      <div className="rounded-xl bg-white border shadow-sm p-6">Unit CRUD with archive lifecycle and landlord isolation.</div>
    </Shell>
  );
}
