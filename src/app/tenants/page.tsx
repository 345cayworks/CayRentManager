import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Tenants">
      <div className="rounded-xl bg-white border shadow-sm p-6">Tenant management with invitation-only onboarding.</div>
    </Shell>
  );
}
