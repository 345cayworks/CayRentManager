import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Tenant Messages">
      <div className="rounded-xl bg-white border shadow-sm p-6">Tenant messaging center with landlord staff.</div>
    </Shell>
  );
}
