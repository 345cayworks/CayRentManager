import { Shell } from '@/components/shell';

export default function Page() {
  return (
    <Shell title="Tenant Lease">
      <div className="rounded-xl bg-white border shadow-sm p-6">Tenant's active lease details only.</div>
    </Shell>
  );
}
